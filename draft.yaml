models:
  Post:
    user_id: id foreign:users
    title: string
    slug: string nullable
    is_active: boolean
    is_featured: boolean
    relationships:
      belongsToMany: Category


controllers:
  Admin/Post:
    resource
  Admin/PostCategory:
    resource

seeders: Blog, Category
